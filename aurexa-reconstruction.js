(()=>{
const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];
let last='';
function go(p){q(`#side [data-page="${p}"]`)?.click()}
function text(sel,root=document){return root.querySelector(sel)?.textContent?.trim()||''}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function rebuildAuth(){const a=q('.auth');if(!a||a.dataset.rebuilt)return; a.dataset.rebuilt='1';a.innerHTML=`<div class="auth-castle"><div class="auth-moon"></div><div class="auth-tower t1"></div><div class="auth-tower t2"></div><div class="auth-tower t3"></div><div class="auth-gem">◇</div><div class="auth-royal"><div class="auth-crown">♛</div><h1>AUREXA</h1><p>EL REINO DE LOS DIAMANTES</p></div></div><section class="auth-card">${a.querySelector('.auth-card')?.innerHTML||''}</section>`;}
function home(){const v=q('#view');if(!v||v.dataset.rebuilt)return;v.dataset.rebuilt='1';const old=v;const d=text('.balance strong',old).replace(/^◇\s*/,'')||'0';const cup=text('.cup strong',old)||'0';const energy=text('.stat b',old)||'0';old.innerHTML=`<div class="reference-home"><div class="realm-mast"><div><div class="chapter">REINO DE DIAMANTES</div><h1>AUREXA</h1></div><div class="sigil">♛</div></div><section class="hero-scene"><div class="scene-back"></div><div class="scene-arch"></div><div class="scene-crystal"></div><img class="scene-wizard" src="/assets/aurexa-wizard-royal.svg" alt="Hechicera del reino"><span class="scene-gem g1">✦</span><span class="scene-gem g2">◇</span><span class="scene-gem g3">♛</span><span class="scene-gem g4">◇</span><div class="scene-copy"><div class="crest">♛</div><h2>BIENVENIDA<span>A</span>AUREXA</h2><p>El reino te espera. Mina, descubre tesoros y construye tu fortuna virtual.</p><button class="scene-cta" data-go="mining">COMENZAR →</button></div></section><div class="home-ribbon"><div class="royal-stat"><span>DIAMANTES</span><strong>◇ ${esc(d)}</strong><small>tesoro virtual</small></div><div class="royal-stat"><span>SALDO CUP</span><strong>${esc(cup)}</strong><small>saldo disponible</small></div><div class="royal-stat"><span>ENERGÍA</span><strong>${esc(energy)}</strong><small>poder para minar</small></div></div><div class="realm-doors"><article class="realm-door"><div class="door-art"></div><div class="door-copy"><h3>MINA</h3><p>Extrae diamantes.</p><button data-go="mining">ENTRAR</button></div></article><article class="realm-door"><div class="door-art"></div><div class="door-copy"><h3>TIENDA</h3><p>Adquiere paquetes.</p><button data-go="packages">ENTRAR</button></div></article><article class="realm-door"><div class="door-art"></div><div class="door-copy"><h3>TESORO</h3><p>Consulta tus gemas.</p><button data-go="wallet">ENTRAR</button></div></article><article class="realm-door"><div class="door-art"></div><div class="door-copy"><h3>TRONO</h3><p>Tu perfil del reino.</p><button data-go="profile">ENTRAR</button></div></article></div></div>`;bindGo()}
function mining(){const v=q('#view');if(!v||v.dataset.rebuilt)return;const oldBtn=v.querySelector('#mineBtn'),oldOut=v.querySelector('#mineOut'),rows=qa('.row',v).map(r=>`<div class="ornate-row"><span>${esc(text('span',r))}</span><b>${esc(text('b',r))}</b><small>${esc(text('small',r))}</small></div>`).join('');v.dataset.rebuilt='1';v.innerHTML=`<div class="screen-frame"><div class="screen-inner"><div class="screen-kicker">LA MINA</div><h1 class="screen-title">CÁMARA DE CRISTAL</h1><p class="screen-subtitle">El servidor determina la duración y recompensa.</p><div class="mine-stage"><div class="mine-altar"><div class="mine-gem"></div></div><div class="mine-copy"><div class="screen-kicker">CORAZÓN DE LA MONTAÑA</div><h2>10 ◆ / HORA</h2><p>Entra en la cámara y extrae diamantes virtuales. Cada operación se valida en el servidor.</p><div id="rebuildMineAction"></div><div id="rebuildMineOut"></div></div></div>${rows?`<div class="ornate-list">${rows}</div>`:''}</div></div>`;if(oldBtn){oldBtn.className='game-button';q('#rebuildMineAction').appendChild(oldBtn)}if(oldOut)q('#rebuildMineOut').appendChild(oldOut)}
function wallet(){const v=q('#view');if(!v||v.dataset.rebuilt)return;v.dataset.rebuilt='1';const val=text('.wallet-main strong',v).replace(/^◇\s*/,'')||'0';const rows=qa('.wallet-main~.card .row',v);v.innerHTML=`<div class="screen-frame"><div class="screen-inner"><div class="screen-kicker">TESORO</div><h1 class="screen-title">CÁMARA DEL TESORO</h1><p class="screen-subtitle">Tus diamantes permanecen dentro de AUREXA.</p><div class="treasure-layout"><div class="treasure-core"><img src="/assets/aurexa-diamond.svg" alt="Diamante"></div><div class="treasure-value"><span>DIAMANTES DISPONIBLES</span><strong>◇ ${esc(val)}</strong><p>Saldo virtual sin valor monetario.</p><div class="treasure-actions"><button class="game-button" data-go="packages">COMPRAR</button><button class="game-button" data-go="history">MOVIMIENTOS</button></div><div class="inventory-list">${rows.map(r=>`<div class="inventory-item"><span>${esc(text('span',r))}</span><b>${esc(text('b',r))}</b></div>`).join('')}</div></div></div><div class="notice"><b>ECONOMÍA VIRTUAL</b><p>Los diamantes solo existen dentro de la aplicación y no pueden retirarse, convertirse ni canjearse por dinero real.</p></div></div></div>`;bindGo()}
function market(){const v=q('#view');if(!v||v.dataset.rebuilt)return;v.dataset.rebuilt='1';const products=qa('.product',v).map(p=>{const buy=p.querySelector('[data-buy]');return {name:text('h3',p),amount:text('strong',p),price:text('.price',p),popular:!!p.querySelector('em'),buy}});v.innerHTML=`<div class="market-scene"><div class="market-intro"><div class="screen-kicker">MERCADO REAL</div><h1>TIENDA DEL REINO</h1><p>Paquetes procedentes del catálogo del servidor.</p></div><div class="royal-products">${products.length?products.map((p,i)=>`<article class="royal-product">${p.popular?'<em>MÁS POPULAR</em>':''}<h3>${esc(p.name)}</h3><div class="amount">${esc(p.amount)}</div><div class="price">${esc(p.price)}</div><div class="buy-slot"></div></article>`).join(''):'<p class="muted">El catálogo todavía no está disponible. No se muestran paquetes inventados.</p>'}</div><div class="notice"><b>PAGO MANUAL</b><p>La compra se coordina por el canal oficial y la acreditación requiere aprobación.</p></div></div>`;products.forEach((p,i)=>{if(p.buy)qsa('.royal-product')[i]?.querySelector('.buy-slot')?.appendChild(p.buy)});qa('[data-buy]').forEach(b=>{b.className='game-button';b.textContent='COMPRAR'})}
function qsa(s){return qa(s)}
function listScreen(title,kicker){const v=q('#view');if(!v||v.dataset.rebuilt)return;v.dataset.rebuilt='1';const panels=qa('.card',v);let rows=[];panels.forEach(p=>qa('.row',p).forEach(r=>rows.push(`<div class="ornate-row"><span>${esc(text('span',r))}</span><b>${esc(text('b',r))}</b><small>${esc(text('small',r))}</small></div>`)));v.innerHTML=`<div class="screen-frame"><div class="screen-inner"><div class="screen-kicker">${kicker}</div><h1 class="screen-title">${title}</h1><div class="ornate-list">${rows.length?rows.join(''):'<div class="ornate-row"><span>No hay registros todavía.</span></div>'}</div></div></div>`}
function profile(){const v=q('#view');if(!v||v.dataset.rebuilt)return;v.dataset.rebuilt='1';const name=text('.profile-card h2',v)||'Jugador',id=text('.profile-card p',v),stats=qa('.profile-card .stat',v);v.innerHTML=`<div class="screen-frame"><div class="screen-inner"><div class="screen-kicker">JUGADOR</div><h1 class="screen-title">SALÓN DEL TRONO</h1><p class="screen-subtitle">Tu identidad dentro del reino.</p><div class="profile-scene"><div class="royal-portrait"></div><div class="royal-profile"><div class="screen-kicker">AVENTURERO</div><h2>${esc(name)}</h2><p>${esc(id)}</p><div class="profile-stat-grid">${stats.map(s=>`<div class="profile-stat"><span>${esc(text('span',s))}</span><b>${esc(text('b',s))}</b></div>`).join('')}</div></div></div></div></div>`}
function support(){const v=q('#view');if(!v||v.dataset.rebuilt)return;const btn=v.querySelector('#supportBtn');v.dataset.rebuilt='1';v.innerHTML=`<div class="screen-frame"><div class="screen-inner"><div class="screen-kicker">CÁMARA REAL</div><h1 class="screen-title">SOPORTE</h1><p class="screen-subtitle">Ayuda oficial para tu cuenta.</p><div class="support-realm"><section class="realm-panel"><h3>MENSAJERÍA REAL</h3><p>Contacta el canal oficial configurado por AUREXA para recibir asistencia.</p><div id="support-slot"></div></section><section class="realm-panel"><h3>SELLO DE SEGURIDAD</h3><p>Nunca compartas tu contraseña ni códigos de verificación. Las operaciones sensibles se validan en servidor.</p></section></div></div></div>`;if(btn){btn.className='game-button';q('#support-slot').appendChild(btn)}}
function settings(){const v=q('#view');if(!v||v.dataset.rebuilt)return;v.dataset.rebuilt='1';v.innerHTML=`<div class="screen-frame"><div class="screen-inner"><div class="screen-kicker">CUENTA</div><h1 class="screen-title">CONFIGURACIÓN</h1><p class="screen-subtitle">Preferencias y seguridad.</p><div class="settings-realm"><section class="realm-panel"><h3>ESTADO DE CUENTA</h3><div class="setting-row"><span>Correo verificado</span><b>SÍ</b></div><div class="setting-row"><span>Autenticación</span><b>FIREBASE</b></div><div class="setting-row"><span>Economía</span><b>VIRTUAL</b></div></section><section class="realm-panel"><h3>REGLAS DEL REINO</h3><p>Tu progreso y saldo se consultan desde el servidor. Las operaciones sensibles permanecen protegidas.</p></section></div></div></div>`}
function bindGo(){qa('[data-go]').forEach(b=>b.onclick=()=>go(b.dataset.go))}
function run(){rebuildAuth();const v=q('#view');if(!v)return;const active=q('#side button.active')?.dataset.page||'';if(active==='home')home();else if(active==='mining')mining();else if(active==='wallet')wallet();else if(active==='packages')market();else if(active==='history')listScreen('HISTORIAL','ARCHIVOS DEL REINO');else if(active==='profile')profile();else if(active==='support')support();else if(active==='settings')settings();else if(active==='notifications')listScreen('NOTIFICACIONES','CUENTA');}
const mo=new MutationObserver(()=>{clearTimeout(window.__aurexaRebuildTimer);window.__aurexaRebuildTimer=setTimeout(run,20)});mo.observe(document.body,{childList:true,subtree:true});setTimeout(run,100);
})();
